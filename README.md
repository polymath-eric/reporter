A node script intended to email regular reports on created and registered tickers on Polymesh.

To use, set appropriate constants in `report.js` and use `node report.js` (install dependencies with `npm i`)
